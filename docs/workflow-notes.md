# Workflow Notes

1 develop all changes on dedicated feature branches to keep the main branch stable
2 peer reviews are required for every pr before merge to ensure code quality
3 all the commits should be meaningful
4 env should be managed through env files and never be hardcoded
5 avoid force pushing to shared repos

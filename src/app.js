// Simple starter application.
// It runs, but it is not finished.

// TODO: replace placeholder implementation

const APP_PORT = process.env.APP_PORT || 8080; // TODO: read this from environment config instead of hardcoding

function main() {
  // TODO: replace placeholder implementation
  const message = "app has been successfully started and is listening on port " + APP_PORT;
  console.log(message);
  console.log("listening on port " + APP_PORT);
}

main();
